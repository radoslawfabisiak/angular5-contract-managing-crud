import { ExperienceYearsPipe } from './experience-years.pipe';

describe('ExperienceYearsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienceYearsPipe();
    expect(pipe).toBeTruthy();
  });
});
